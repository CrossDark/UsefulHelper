# UsefulTools
>Some python tools
## Manage
>setup:setup settings
pack:pack and upload to pypi
build:build project
(```)
build(
    _type_='SQL',
    temple='SQLite'
)
(```)
stop:stop manage.py
clean:clean project
